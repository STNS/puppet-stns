HOSTS:
  ubuntu-trusty-x64:
    platform: ubuntu-trusty-x86_64
    image: ubuntu:trusty
    hypervisor: docker
    docker_image_commands:
      - 'apt-get install -y sudo wget'
CONFIG:
  type: foss
  masterless: true
